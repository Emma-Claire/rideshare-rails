<body>
  <h1>Passengers' List</h1>
  <button class="mdl-button mdl-js-button mdl-button--raised">
    <%= link_to "Add new passenger", new_passenger_path %>
  </button>

  <section>
    <% @passengers.each do |passenger| %>
    <%= link_to passenger.name, passenger_path(passenger.id) %>
    <!-- <%= "Name: #{passenger[:name]}"%> -->
    <%= link_to "✏️", edit_passenger_path(passenger) %>
    <%= link_to "🗑", p, method: :delete, class: "fa-trash-o", data: {confirm: "Are you sure?"} %>
  <%= link_to "Trips", passenger_trips_path(passenger) %></br>
  <% end %>
  </section>

  </br>
  <button class="mdl-button mdl-js-button mdl-button--raised">
    <%= link_to "Add new passenger", new_passenger_path %>
  </button>
</body>
